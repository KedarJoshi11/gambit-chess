const TIMES=['10:00 AM','1:00 PM','4:00 PM','7:00 PM'];
export async function onRequestGet({request,env}){const url=new URL(request.url),coachId=Number(url.searchParams.get('coachId')),date=url.searchParams.get('date');if(!Number.isInteger(coachId)||!/^\d{4}-\d{2}-\d{2}$/.test(date||''))return Response.json({error:'A valid coach and date are required.'},{status:400});const {results}=await env.DB.prepare("SELECT session_time FROM bookings WHERE coach_id=? AND session_date=? AND status!='cancelled'").bind(coachId,date).all();return Response.json({slots:TIMES.filter(time=>!results.some(row=>row.session_time===time))})}
